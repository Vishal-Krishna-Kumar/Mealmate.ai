# Core
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# ML / data — unpinned to grab latest cp314 wheels
scikit-learn
pandas
numpy
joblib
nltk

# HTTP / observability
httpx>=0.27
structlog>=24.4
prometheus_client>=0.20

# Optional LLM provider — Google Gemini.
# The SDK is imported lazily so the service still runs without it; only
# required when GEMINI_API_KEY is set and you want LLM-backed features.
google-generativeai>=0.8
